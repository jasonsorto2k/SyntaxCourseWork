package class2;

public class VariablesNumbers {

    public static void main(String[] args) {

        //Variable are storage locations with assigned names, that are data holders you use later on
        //Variables hold 'Data Types'
        //'DataTypes' defines the type of data that a 'variable' can hold
        //DataTypes goes as follows-Integer,character,byte,string,short,long,double,boolean,float

        //Use double slash for any note-taking & italics for long passages of notes
        //Shortcuts Command V (paste), C (copy), D (Duplicate)
        //Shortcuts main line, type 'line' and "S.o.u.t" for "system.print.out"

        // Data Type - Variable Name = Value
        // All data gets stored in Cpu's RAM

        // Byte, Short, Int, Long - these data types all hold whole number

        // Each Data Type that holds whole numbers, each has different capacity

        byte box2 = 127;
        byte box3 = -128;
        // range for byte is from -128 to 127

        short box4 = 32767;
        short box5 = -32768;
        // minimum of -32,768 and a maximum value of 32,767

        int biggerBox = 123456784;
        // This data type is used most commonly because of its range

        long maxBox = 15131351465464L;
        // minimum value of -263 and a maximum value of 263-1 marked with an 'L'

        short myBox = 10000;
        System.out.println(myBox);


    }
}
